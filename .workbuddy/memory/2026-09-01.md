# 2026-09-01

## GitHub 推送配置（已完成）
- 项目已推送到 https://github.com/falcon9-9/just-listen（公开仓库，main 分支）
- 内容：docs/brand.md、docs/需求文档.md
- SSH key：~/.ssh/id_ed25519_github（账户 falcon9-9），~/.ssh/config 已配置 Host github.com
- gh CLI 已安装（brew），凭证存于 ~/.config/gh（keyring）
- 坑：沙箱会拦截 ~/.ssh、~/.config 的写入及后台任务的 keychain 访问；解决方式：先写 /tmp 再 cp（dangerouslyDisableSandbox），gh auth login 需在沙箱放行 ~/.config 后才能保存 token
